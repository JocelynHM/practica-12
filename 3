#include<stdio.h>
int c, x, y ; 
int mostrar(int 
{
	for(c=0; c<x; c++)
	{
		printf(“%d”, c]);
	}
	return 0;
}
main()
{
	int a, b , c; 
  //Leer un arreglo de N números, en otro arreglo almacenarlo ordenado en forma ascendente y en otro almacenarlo en forma descendente. Mostrar los tres arreglos.
  printf(" Este programa lee un arreglo de tamaño elegido por el usuario, lo almacena en forma ascendente y descendente y lo muestra. 
  
