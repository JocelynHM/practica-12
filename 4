#include<stdio.h>

int c, a15];
/* Asignarle 15 datos a un arreglo en un rango, esto es por medio de la función de números aleatorios, por medio de la misma función 
generar un número dentro del mismo rango. Mostrar el arreglo y cuantas veces se encontró el número.*/

int rellenar(a15])
{
  for (c=0; c<15; c++)
  {
    a[c]= (rand()%100);
  }
  return 0;
}

int mostrar (a[15])
{
  for(c=0; c<15; c++)
  {
    printf("%d t", a[c;
  }
  return 0;
 }
 int buscar ()
 {
  for (c=0; c<15; c++)
  {
    if (a 15 ==n)
      b++;
  }
  return 0;
 }
 
int main()
{
  int n;
  system("color f0");
  srand(NULL);
  n= (rand()%100);
  printf(" Este programa le asigna15 numeros aleatorios a un arreglo y al mismo tiempo, genera un numero que buscara el arreglo y dira cuantas veces aparece. Por HERNANDEZ JOCELYN. ");
  printf(" El arreglo de numeros alatorios es: ");
  mostar();
  printf("el numero alatorio a buscar es "%d", n); 
  buscar();
  if (b==0)
    printf("el numero %d no se encuentra ninguna vez en el arreglo"); 
  else
  
    printf( "el numero %d aparecio %d veces", n, b); 
   
   return 0; 
  }
  
  
